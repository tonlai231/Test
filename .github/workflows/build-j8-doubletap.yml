name: Build J8 Double Tap Sleep APK

on:
  push:
    branches: ["chatgpt-j8-doubletap-sleep"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'
      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace
      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk J8-DoubleTap-Sleep.apk
      - uses: actions/upload-artifact@v4
        with:
          name: J8-DoubleTap-Sleep-APK
          path: J8-DoubleTap-Sleep.apk
          if-no-files-found: error
